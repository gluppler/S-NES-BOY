MEMORY {
    ZEROPAGE: start = $0000, size = $0100, type = rw, file = "";
    HEADER: start = $7FB0, size = $0050, fill = yes, fillval = $00, file = %O;
    VECTORS: start = $7FE4, size = $0018, fill = yes, fillval = $00, file = %O;
    PRG: start = $8000, size = $8000, fill = yes, fillval = $00, file = %O;
}

SEGMENTS {
    ZEROPAGE: load = ZEROPAGE, type = rw, optional = yes;
    HEADER: load = HEADER, type = ro;
    VECTORS: load = VECTORS, type = ro;
    CODE: load = PRG, type = rw, start = $8000;
}
